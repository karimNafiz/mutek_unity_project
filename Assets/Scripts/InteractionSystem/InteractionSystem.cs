using UnityEngine;

public class InteractionSystem : MonoBehaviour
{
    [SerializeField] private float rayCastLength;
    // have a serialized field for the layer





    // have the raycast sensor 

    // late update cuz its physics stuff
    // every late update, do the raycast
    // have a layer called InteractionLayer
    // the raycast should hit the interaction layer
}
